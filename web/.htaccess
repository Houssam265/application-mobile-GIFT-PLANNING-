RewriteEngine On

# Si la requête cible un vrai fichier ou dossier, servir tel quel.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, rediriger vers l'entrée Flutter Web.
RewriteRule ^ index.html [L]
